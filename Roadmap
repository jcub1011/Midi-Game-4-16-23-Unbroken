TODO:
Implement way of detecting input.
Implement way of checking if input matches what is currently supposed to be played.
Replace collision ranges with checking if the edge is within the strike range. Probably how it should have been done from the get go.
Implement way of scrubbing through songs and selecting what channels(s) to play and what will be played automatically.
